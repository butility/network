export * from '@/core/request';
import { ajax, get, patch, post, put, deleteRequest } from '@/core/request';

const Request = {
    ajax,
    get,
    patch,
    post,
    put,
    deleteRequest,
};

export default Request;
